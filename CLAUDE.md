# Claude Code Routing

This file is auto-loaded by Claude Code every session. Routing only — governing content lives in `docs/law/*` and root control documents.

## Governance Reminder

Before starting fix / init / upgrade / align / governance work (tracker-policy violation, repeated mistake, rule bypass, new project setup, kit upgrade, local harness routing drift), read the matching root control document first: `AGENTLAW_FIX_TOOL.md` for fix-class work, `AGENTLAW_INIT_TOOL.md` for init, `AGENTLAW_UPDATE_TOOL.md` for kit upgrades, `AGENTLAW_ALIGN_TOOL.md` for local routing/readme alignment. The `.claude/skills/agentlaw-governance/SKILL.md` skill carries the same reminder and activates by description-matching when the user's request contains trigger keywords (fix, init, upgrade, align, 고치자, 초기화, 업그레이드, governance gap, rule violation).

## Same content as AGENTS.md

This file mirrors the routing in `AGENTS.md`. Claude Code reads `CLAUDE.md` automatically every session; Codex reads `AGENTS.md`. Both files share the governance reminder above. See `AGENTS.md` for the full routing map (Source of Truth, Read First, Memory layer, etc.).
