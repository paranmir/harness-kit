# Local Rules

This directory holds project-local governance rules authored via `memory_save_item(type="rule", ...)`. See `agentlaw_docs/law/MEMORY_AND_CONTINUITY_RULES.md` ("Local Rule Memory Type") for the framework.

The starter seed ships this directory empty. Populate it with your own rules after install — they stay local to your project and never propagate back upstream.
